extends layout 
include mixins/_user_form
block content 

    .signup_main
        h2= title
        hr
        if errors 
            ul
                for error in errors 
                    li= error.msg
        +userForm()